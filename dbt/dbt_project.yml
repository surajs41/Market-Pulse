name: marketpulse
version: 1.0.0
config-version: 2

profile: marketpulse

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  marketpulse:
    staging:
      +materialized: view
    marts:
      +materialized: table
